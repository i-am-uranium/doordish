name: doordish
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  universal_html: ^1.2.2
  flutter_icons: ^1.1.0
  logger: ^0.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/navigation_rail_bg.png
    - assets/icons/calendar.png
    - assets/icons/faq.png
    - assets/icons/menu.png
    - assets/icons/overview.png
    - assets/icons/testimonial.png
    - assets/icons/transaction.png
