import 'package:flutter/material.dart';

class EditDeliveryDetails extends StatelessWidget {
  const EditDeliveryDetails({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
